name: hydration
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  geolocator: ^10.1.0
  shared_preferences: ^2.2.2
  flutter_local_notifications: ^17.0.0

  # ✅ NEW (animated water level)
  liquid_progress_indicator_v2: ^0.5.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Add assets section if you want to include images
  assets:
    - assets/images/water_droplet.png  # Ensure the water droplet image is in your assets folder